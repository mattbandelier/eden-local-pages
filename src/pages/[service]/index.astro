---
import BaseLayout from "../../layouts/BaseLayout.astro";

// Service hub page (e.g. /botox)
// Placeholder route — Phase 3 will populate getStaticPaths from
// src/data/services.json.
export async function getStaticPaths() {
	return [];
}

const { service } = Astro.params;
---

<BaseLayout
	title={`Coming soon — ${service} hub page`}
	description={`Eden Health Clubs ${service} services. Coming soon.`}
	canonicalUrl={`https://landing.edenhealthclubs.com/${service}`}
>
	<main class="flex min-h-screen items-center justify-center px-6">
		<p class="text-eden-navy/80">Coming soon: {service} hub page</p>
	</main>
</BaseLayout>
